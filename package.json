{
  "name": "capsule",
  "version": "0.1.0",
  "private": true,
  "description": "Cross-platform photo sharing with decentralized storage",
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/web",
    "build": "npm run build --workspace=apps/web",
    "lint": "npm run lint --workspace=apps/web",
    "db:migrate": "supabase db push",
    "db:types": "supabase gen types typescript --project-id kjdoiozqefbjkbsimvbs > apps/web/src/types/database.ts",
    "db:reset": "supabase db reset",
    "ios": "open apps/ios/Capsule.xcodeproj"
  },
  "devDependencies": {
    "supabase": "^1.200.0"
  }
}
